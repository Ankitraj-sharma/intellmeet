{
  "name": "intellmeet",
  "version": "2.0.0",
  "description": "IntellMeet – AI-Powered Enterprise Meeting & Collaboration Platform",
  "private": true,
  "scripts": {
    "dev": "make dev",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "test": "cd backend && npm test",
    "seed": "cd backend && node scripts/seed.js",
    "lint": "cd backend && npm run lint --if-present && cd ../frontend && npm run lint --if-present",
    "docker:up": "docker compose up --build -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f"
  },
  "keywords": ["mern", "meetings", "ai", "webrtc", "socket.io", "enterprise"],
  "author": "Zidio Development",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
